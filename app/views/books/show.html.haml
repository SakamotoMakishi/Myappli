= render 'books/header'#ヘッダーのビューファイル
.top_page
  .contents_section
    .container
      .header-section
        %h2.text-middle
          %i.fas.fa-book
          CONTENTS
    .book_list
      .text-middle
        %i.fas.fa-dice
        = @book.title
    = image_tag "#{@book.image}", alt: "#{@book.title}",class: 'one_image'
  //products/<haml_loud> @product.id </haml_loud>/reviews/new映画idを取得してreviews/nerwに渡している